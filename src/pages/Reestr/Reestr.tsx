import React, { FC } from "react";
import Header from "../../components/Header/Header";
import ReestrComponent from "../../components/ReestrComponent/ReestrComponent";

const Reestr: FC = () => {
  return (
    <div>
      <Header />
      <ReestrComponent />
    </div>
  );
};

export default Reestr;
